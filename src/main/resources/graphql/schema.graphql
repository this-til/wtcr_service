type Query {
    getUserById(id : String) : User
    getUserByAccount(id : String) : User
}


type User {
    id: ID!
    account: String!
    name: String
    email: String
    phone: String
    headPath: String
    gender: String!
    permission: String!
}